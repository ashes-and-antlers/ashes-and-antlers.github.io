# Ashes and Antlers — local development environment
# Copy to .env and adjust as needed. All values have working defaults.

# PostgreSQL (matches docker-compose.yml)
DATABASE_URL=postgres://ashes:ashes@localhost:5432/ashes

# API
PORT=3001
WORLD_SEED=1337
TICK_DURATION_MS=1800000
PLAYER_TOKEN=player-1337-token
ADMIN_TOKEN=dev-admin-token
